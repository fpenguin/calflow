# CalFlow development dependencies.
#
# Install with:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# These are NOT needed at runtime — only for tooling, lint, and tests.

# Linter + formatter (CI uses these as advisory checks).
ruff>=0.5.0

# Pre-commit hook framework (see .pre-commit-config.yaml).
pre-commit>=3.5.0

# Static type checker (planned for v1.1.27 strict pass).
mypy>=1.8.0

# Test framework (we use unittest today; pytest is planned for v1.1.27).
pytest>=8.0.0
pytest-cov>=4.1.0
