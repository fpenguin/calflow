# CalFlow runtime dependencies (pinned for reproducible installs).
#
# Regenerate from a clean venv:
#     python3 -m venv .venv
#     source .venv/bin/activate
#     pip install \
#         google-api-python-client \
#         google-auth \
#         google-auth-oauthlib \
#         python-dateutil
#     pip freeze > requirements.txt
#
# Direct deps (everything else is a transitive pin):
#   google-api-python-client  — Google Calendar API client
#   google-auth               — OAuth 2.0 + token refresh
#   google-auth-oauthlib      — InstalledAppFlow for the desktop OAuth dance
#   python-dateutil           — ISO 8601 parsing of event start/end fields
#
# Menubar deps (v1.3.0 — only required for `python -m cli.main menubar`;
# CLI keeps working without them via lazy imports):
#   pyobjc-framework-Cocoa     — NSStatusBar, NSPopover, NSApplication
#   pyobjc-framework-WebKit    — WKWebView for the popover UI
#   rumps                      — convenience wrappers (currently unused;
#                                reserved for future menu-item helpers)
#
# Optional (dev) deps live in requirements-dev.txt.

certifi==2026.2.25
cffi==2.0.0
charset-normalizer==3.4.7
cryptography==46.0.7
google-api-core==2.30.2
google-api-python-client==2.194.0
google-auth==2.49.1
google-auth-httplib2==0.3.1
google-auth-oauthlib==1.3.1
googleapis-common-protos==1.74.0
httplib2==0.31.2
idna==3.11
oauthlib==3.3.1
proto-plus==1.27.2
protobuf==6.33.6
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==2.23
pyparsing==3.3.2
python-dateutil==2.9.0.post0
requests==2.32.5
requests-oauthlib==2.0.0
six==1.17.0
typing_extensions==4.15.0
uritemplate==4.2.0
urllib3==2.6.3

# v1.3.0 — menubar companion (lazy-imported; CLI works without these).
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-WebKit>=10.0
rumps>=0.4.0
